<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.ztl.cool.user.dao.UserDao">
    <!-- 结果集 -->
    <resultMap type="cc.ztl.cool.user.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="age" column="age" jdbcType="INTEGER"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="version" column="version" jdbcType="INTEGER"/>
    </resultMap>
    
    <!-- 基本字段 -->
    <sql id="Base_Column_List">
        id, name, age, email, create_time, update_time, version    </sql>
    
    <!-- 查询单个 -->
    <select id="selectById" resultMap="UserMap">
        select
          <include refid="Base_Column_List" />
        from users
        where id = #{id}
    </select>

    <!-- 查询全部 -->
    <select id="selectAll" resultMap="UserMap">
        select
        <include refid="Base_Column_List" />
        from users
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="selectList" resultMap="UserMap">
        select
        <include refid="Base_Column_List" />
        from users
        <where>
                    <if test="id != null">
                and id = #{id}
            </if>
                    <if test="name != null and name != ''">
                and name = #{name}
            </if>
                    <if test="age != null">
                and age = #{age}
            </if>
                    <if test="email != null and email != ''">
                and email = #{email}
            </if>
                    <if test="createTime != null">
                and create_time = #{createTime}
            </if>
                    <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
                    <if test="version != null">
                and version = #{version}
            </if>
                </where>
    </select>

    <!-- 新增所有列 -->
    <insert id="insert" keyProperty="" useGeneratedKeys="true">
        insert into users(id, name, age, email, create_time, update_time, version)
        values ( #{id}, #{name}, #{age}, #{email}, #{createTime}, #{updateTime}, #{version})
    </insert>
    
    <!-- 批量新增 -->
    <insert id="batchInsert">
        insert into users(id, name, age, email, create_time, update_time, version)
        values 
        <foreach collection="users" item="item" index="index" separator=",">
        (
                        #{item.id},             #{item.name},             #{item.age},             #{item.email},             #{item.createTime},             #{item.updateTime},             #{item.version}         )
         </foreach>
    </insert>

    <!-- 通过主键修改数据 -->
    <update id="update">
        update houtai.users
        <set>
                    <if test="id != null">
                id = #{id},
            </if>
                    <if test="name != null and name != ''">
                name = #{name},
            </if>
                    <if test="age != null">
                age = #{age},
            </if>
                    <if test="email != null and email != ''">
                email = #{email},
            </if>
                    <if test="createTime != null">
                create_time = #{createTime},
            </if>
                    <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
                    <if test="version != null">
                version = #{version},
            </if>
                </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from users where id = #{id}
    </delete>
    
    <!-- 总数 -->
    <select id="count" resultType="int">
        select count(*) from users
    </select>
    <!--分页-->
    <select id="selectPage" resultType="cc.ztl.cool.user.entity.User">
        select
        <include refid="Base_Column_List" />
        from users
        limit #{current},#{size};
    </select>
</mapper>